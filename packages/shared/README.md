# shared

Internal package that contains shared objects such as the content script used by the chrome extension or the rollup plugin, the various types that connects the content script and the "grep" server.
